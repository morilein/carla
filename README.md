# Testing around with the [Carla Simulator `(0.9.8)`](http://carla.org/)

## Docker
Using Docker because of future TensorFlow usage and python3.5 distributables.
To start container simply run

``` bash
./docker_run.sh
```
