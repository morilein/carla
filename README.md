# Testing around with the [Carla Simulator](http://carla.org/)

## Docker
Using Docker because of future TensorFlow usage and python `3.5` distributables.

To start container simply run ```./docker_run.sh```.
